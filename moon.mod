name = "username/moonkit"

version = "0.1.0"

readme = "README.mbt.md"

repository = "https://github.com/username/moonkit"

license = "Apache-2.0"

keywords = ["toml", "semver", "ulid", "strcase", "validator", "utility", "config", "parser"]

preferred_target = "wasm-gc"

description = "A comprehensive utility toolkit for MoonBit, providing TOML parsing, semantic versioning, ULID generation, string case conversion, and data validation."
